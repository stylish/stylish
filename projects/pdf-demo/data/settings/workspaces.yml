workspaces:
  main:
    panels:
      presentation:
        command: 'skypager dev --bundle --port 3000'
        url: "http://localhost:3000"
        window:
          height: 300
          width: 200
      console:
        noServer: true
        url: "http://localhost:3000/console.html"

