import skypager from 'skypager-project'
import { existsSync as exists } from 'fs'
import { join, resolve, dirname, basename } from 'path'

export function createOrLoadFromPath(portfolioRoot) {
  if (!exists(portfolioRoot)) {

  }
}
